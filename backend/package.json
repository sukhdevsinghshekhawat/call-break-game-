{
  "name": "callbreak-multiplayer",
  "version": "1.0.0",
  "private": true,
  "description": "Call Break — multiplayer table for four (LAN party via QR code)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:public": "TUNNEL=true node server.js",
    "test": "node validate.js",
    "smoke": "node smoke.js"
  },
  "dependencies": {
    "localtunnel": "^2.0.2",
    "ws": "^8.18.0"
  }
}
